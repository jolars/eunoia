// Page metadata consumed by the root +layout.svelte through `page.data`.
export function load() {
  return {
    title: "Quickstart: R",
    description:
      "Generate area-proportional Euler and Venn diagrams in R with eulerr, the original Eunoia binding.",
  };
}
