// Page metadata consumed by the root +layout.svelte through `page.data`.
export function load() {
  return {
    title: "Label placement",
    description:
      "How Eunoia positions region labels using poles of inaccessibility, keeping text inside the correct area even for complex overlaps.",
  };
}
