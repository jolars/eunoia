// Page metadata consumed by the root +layout.svelte through `page.data`.
export function load() {
  return {
    title: "Complement",
    description:
      "How Eunoia fits a bounding container (the universe) whose leftover area matches a target, for complement-aware Euler diagrams.",
  };
}
